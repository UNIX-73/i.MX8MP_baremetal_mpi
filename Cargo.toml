[package]
name = "imx8mp_kernel"
version = "0.1.0"
edition = "2024"

[lib]
path = "_src/lib.rs"
crate-type = ["staticlib"]
test = false
doctest = false
bench = false

[profile.release]
panic = "abort"
opt-level = "s"

[dependencies]
